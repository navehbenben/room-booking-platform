# Environment secrets — never commit
#for now i only demonstrate with .env, but in production, we should use vault or something like that

# IDEs
.idea/
.claude/
docs/

# Dependencies
node_modules/

# Build outputs
dist/
*.tsbuildinfo

# macOS
.DS_Store
